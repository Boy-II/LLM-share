# AI 名詞解釋（白話版）

> 這份文件是給**沒有 IT 背景**的人看的 AI 名詞速查。每個詞用最白話的方式講「它是什麼、能幹嘛」，必要時配一個生活比喻或小圖。
>
> 想先搞懂 LLM 的運作原理、Prompt 技巧、RAG 是什麼，請先看同目錄的 `LLM.md`。這份是它的「名詞補充包」。

---

## AI 的兩大家族：文字 AI ╳ 圖像 AI

現在你聽到的 AI，大致可以分成兩個方向，用「它擅長產出什麼」來分最好懂：

```
┌─────────────────────────────┬─────────────────────────────┐
│        文字 AI（LLM）        │      圖像/影音 AI（擴散模型） │
├─────────────────────────────┼─────────────────────────────┤
│ 生成文章、對話、程式碼       │ 生成圖片、影片               │
│ ChatGPT、Claude、Gemini      │ Midjourney、Stable Diffusion │
│ 核心技術：Transformer        │ 核心技術：Diffusion（擴散）  │
└─────────────────────────────┴─────────────────────────────┘
```

> ⚠️ 小提醒：常有人把「擴散模型」跟「卷積模型（CNN）」混為一談，其實不一樣。
> - **擴散模型**：用來「生成」圖片的主流做法（本文第九節）。
> - **CNN（卷積）**：較早期、擅長「看懂」圖片的結構，多用於辨識而非生成。

下面的名詞，前半段（一～八節）大多屬於文字 AI 這邊，第九節是圖像 AI，第十節是其他常見模型。

---

## 一、LLM 基本概念

### Transformer
現在主流 LLM 的核心「大腦結構」（GPT 的「T」就是它）。它最厲害的本事，是讀一句話時能**同時衡量每個字跟其他所有字的關聯**，所以特別擅長理解上下文。第二節的「注意力機制」就是它的核心零件。

> 🕸️ 比喻：不是從頭到尾逐字慢慢讀，而是一眼把整句話的字**全部連起來看關係**，誰跟誰有關一目了然。
>
> 📌 別搞混：你可能也會看到 HuggingFace 的 **Transformers（複數）**——那是一套很紅的**程式庫**，名字取自這個架構，用來下載和執行各種模型。架構是「概念」，函式庫是「工具」。

### 上下文窗口 Context Window
AI 一次對話裡能「同時記得」的文字量上限，超過就會開始忘掉最前面的內容。

> 🪑 比喻：像一張**辦公桌的桌面**。桌面越大，能同時攤開的資料越多；桌面塞滿了，就得把舊資料收走（AI 就「忘了」前面講過的）。

容量用 token 計（token 是什麼見 `LLM.md`）。現在的模型動輒 20 萬 token 起跳，等於能一次讀完好幾百頁文件。

### 資料集 Dataset
拿來「訓練」模型的教材總和，決定了模型懂什麼、不懂什麼。

> 📚 比喻：一個學生從小到大讀過的**所有課本和題庫**。讀過什麼，腦子裡才會有什麼。

### 並發數 Concurrency
同一時間能服務多少個使用者／請求。自己架設模型時很關鍵。

> 🍽️ 比喻：一間餐廳**同時能服務幾桌客人**。並發數越高，越多人同時用也不會卡。

---

## 二、LLM 為什麼跑得快又省：注意力與快取

> 「注意力（Attention）」是 LLM 的核心機制——讀一句話時，模型會判斷**哪些字跟哪些字有關**（例如「牠很可愛」的「牠」指的是前面哪個名詞）。下面這幾個詞，都是讓這個機制**算得更快、更省記憶體**的技術。

### KV Cache（K 快取 / V 快取）
LLM 是一個字一個字生成的。如果每生一個新字，都要把前面所有字從頭重算一遍，會非常慢。KV Cache 把前面算過的中間結果（每個字的 **K**ey 和 **V**alue）**存起來重複利用**，新字直接拿來用，不用重算。

> 📝 比喻：算一道長數學題時，把前面算過的**中間答案寫在草稿紙上**，後面直接拿來用，不用每次從頭再算。
>
> 代價：對話越長，要存的快取越多，**越吃記憶體**。

### Flash Attention
一種重新設計過的「注意力」計算方法，讓 GPU 算得**更快、更省記憶體**，而算出來的結果跟原本一模一樣。

> 👨‍🍳 比喻：同一道菜，**改良過的料理動線**讓出餐更快、廚房更省空間，味道不變。

### SageAttention
在 Flash Attention 之上再進一步，把注意力計算「量化」（降精度，見第四節）來換取**更高速度**，品質幾乎不受影響。

> ⚡ 比喻：Flash Attention 的**省力升級版**，再壓一點點精度，換更快的速度。

### SDPA（Scaled Dot-Product Attention）
注意力計算的「**標準內建版本**」（深度學習框架 PyTorch 內建的基本實作）。Flash、Sage 這些都是它的加速變體。

> 🔧 比喻：**原廠標配的引擎**；Flash／Sage 則是改裝強化版。沒裝加速套件時，跑的就是它。

---

## 三、把模型「養」成你要的樣子：訓練與微調

### 微調 Fine-tune
在一個已經訓練好的通用模型上，再用特定領域的資料「補課」，讓它更擅長某件事（例如法律、醫療、客服語氣）。

> 🎓 比喻：一個**通才員工**，再送去上專業訓練班，變成某領域的專家。底子不變，但更對口。

### LoRA / QLoRA
微調的「省錢省力版」。不去動龐大的原始模型，只在旁邊外掛一小塊「補丁」參數來學新東西。

- **LoRA**：只訓練那塊小補丁，速度快、檔案小，可隨插隨拔。
- **QLoRA**：先把模型壓縮（量化，見第四節）再做 LoRA，更省記憶體，**一張一般顯卡就能跑**。

> 🧥 比喻：不重做整件西裝，只縫一塊**可拆換的內襯**。想換風格就換內襯，西裝本體不動。

---

## 四、讓模型跑在自己電腦上：檔案格式與量化

### GGUF
一種把整個模型打包成**單一檔案**的格式，方便在自己電腦上載入（尤其搭配 llama.cpp）。

> 📦 比喻：把一整套軟體打包成**一個安裝檔**，下載下來就能用。

### 量化 Quantization
把模型內部數字的精度刻意「降級」，換取更小的體積和更快的速度，代價是損失一點點品質。

> 🎵 比喻：把**無損音樂壓成 MP3**。檔案小很多，多數人聽不太出差別。

### FP8／BF16／Q8_0（精度三兄弟）
這些都是「精度等級」的標示，決定模型壓縮多少：

```
BF16   ── 接近原始全精度，品質最好、檔案最大、最吃硬體
 ↓
FP8    ── 壓一半，更小更快，品質掉一點
 ↓
Q8_0 / Q4 …── GGUF 的量化等級，數字越小壓越兇、檔案越小、品質越省
```

> 🖼️ 比喻：同一張圖的**原圖 → 高畫質 → 省流量版**。看你硬體扛得住哪一級。

### mmproj
一個外掛的小檔案，作用是讓原本只懂文字的 LLM「**看得懂圖片**」，把圖片翻譯成模型能理解的訊號。常跟 GGUF 一起出現。

> 👓 比喻：幫一個只懂文字的人，配一位「**把圖畫成文字描述**」的隨身翻譯。（這就是「多模態」的零件，見第十節）

---

## 五、跑模型的「引擎」

模型檔案本身不會動，要靠「推論引擎」把它跑起來。常見三種：

### llama.cpp
輕量工具，能在**自己電腦**（甚至沒有高階顯卡）上跑 LLM，吃的就是 GGUF 檔。

> 🚗 比喻：**家用版的引擎**，目標是「在我自己的車庫就能發動」。

### vLLM
為**伺服器、多人同時使用**設計的高效能引擎，吞吐量大。

> 🏭 比喻：餐廳的**中央廚房**，一次出很多單也不亂。

### SGLang
跟 vLLM 同類，也是高效能伺服器引擎，擅長複雜流程和結構化輸出。它的招牌是把 KV Cache（第二節）整理成一棵**緩存樹（Radix Tree）**——把很多請求**共同的開頭**只算一次、存成一棵共用的樹，省去重複計算。

> 🌳 比喻：很多訂單開頭都點同一份「招牌湯底」，廚房就把湯底**熬一次共用**，誰點到就直接舀，不用每桌從頭重熬。

**緩存命中 Cache Hit**：當新請求要的內容**剛好已經在快取裡**，直接拿來用、不用重算，就叫「命中」；沒對上就是「**未命中 Cache Miss**」，得乖乖重算一次。緩存樹的價值，就是讓共用開頭的請求更容易命中。

> 🎯 比喻：你要查的資料**剛好同事桌上就有**，借來看一眼就好（命中）；不在，才得自己跑一趟資料庫（未命中）。

> 💰 不只地端有：雲端 **API（如 Claude、GPT）也有緩存命中**，叫「**Prompt 快取**」。如果你這次請求的開頭跟前面某次相同（例如重複的系統提示、同一份長文件），命中的那部分**收費會便宜很多**（常只要原價的一小部分），速度也更快。所以重複的內容放在提示詞「開頭」，比較容易吃到這個折扣。

---

## 六、安裝與依賴：自己架 AI 工具時會撞到的詞

當你想在自己電腦上裝 AI 工具（ComfyUI、llama.cpp…），常會冒出一堆安裝相關的詞。不用全記，抓概念就好。

### 依賴 Dependencies
一個軟體要能跑，背後需要的其他套件和工具，少一個都跑不起來。

> 🧑‍🍳 比喻：食譜的**食材清單**，缺一樣就做不出那道菜。

### 依賴地獄 Dependency Hell
不同 AI 工具要的套件版本常常**互相打架**：A 工具要某套件的舊版，B 工具偏要新版，裝了一個就弄壞另一個，怎麼喬都喬不攏。這種卡死狀態就叫依賴地獄，是自己架 AI 工具最常見的痛。

> 🧩 比喻：兩塊拼圖都想佔同一個位置，硬塞整盤就散掉。下面的 venv、容器，就是為了把它們各自隔開、不要打架而生的。

### 套件管理工具 Package Manager
自動幫你下載、安裝、更新那些依賴的工具，不用自己一個一個找。**每個生態系各有一套**，名字不同但做的事一樣：

```
Python   pip（搭配 wheel：預先打包好的安裝檔，裝起來快、不用當場編譯）
Node.js  npm、bun（bun 是比較新、更快的替代品）
macOS    brew（萬用安裝器，一行指令裝各種工具）
```

> 📦 比喻：不同國家的**外送 App**，介面不一樣，但都是幫你把東西送到家。

> 🧱 補充 **wheel（.whl）**：Python 套件「預先編譯打包好」的安裝檔，下載就能裝、不用當場編譯，所以快。你常看到的 PyTorch 配特定 CUDA 版本（例如 `cu121`），其實就是在挑對應的 wheel——**挑錯就是依賴地獄的起點**。

### venv（虛擬環境）
Python 的「依賴隔離區」，讓每個專案的套件各管各的，不會互相打架。

> ⚠️ 再次提醒：venv 隔離的是**依賴**，——它擋不住程式動到電腦其他地方。真正的沙箱見第七節的權限說明。

### WSL（Windows Subsystem for Linux）
讓 Windows 電腦裡跑一個 Linux 環境。很多 AI 工具原本是為 Linux 寫的，WSL 讓 Windows 使用者也能順利安裝。

> 🚪 比喻：在 Windows 裡開一個「**Linux 房間**」，需要 Linux 才能跑的工具就進那個房間。

### 框架與顯卡驅動：PyTorch / CUDA / ROCm
這幾個是「讓 AI 真正用到**顯卡算力**」的底層零件，也是依賴地獄的重災區（版本不合超常見）：

- **PyTorch**：目前最主流的深度學習框架，幾乎所有 AI 工具都建在它上面。你裝 AI 工具，常常就是在裝它。
- **CUDA**：**NVIDIA（N 卡）** 顯卡的加速平台。有它，AI 才能用顯卡算力跑得快；版本要跟 PyTorch 對上，不然會出錯。
- **ROCm**：**AMD（A 卡）** 這邊對應 CUDA 的東西。用 A 卡跑 AI 會用到它（支援度通常不如 N 卡的 CUDA 廣）。

> 🎮 比喻：PyTorch 是「**遊戲**」，CUDA / ROCm 是「**顯卡驅動**」。驅動版本不對，遊戲就跑不動——這正是最常見的依賴地獄來源之一。

---

## 七、你從哪裡用到 AI：使用形態

同一個 AI 可以用不同「介面」碰到它。真正的差別不在介面長相，而在**它能動到你電腦多少東西**。三種形態剛好是一條光譜：

```
              web 端            desktop app           CLI / IDE
           （網頁）            （桌面程式）         （終端機 / 編輯器外掛）
        ┌──────────────┬──────────────────┬────────────────────────┐
本機接觸 │ 對本機無侵入  │ 預設沙箱           │ 高權限                  │
        │ 碰不到你檔案  │ 在隔離區內活動     │ 能讀寫檔案、跑系統指令  │
        ├──────────────┼──────────────────┼────────────────────────┤
適合誰   │ 一般使用者    │ 想多一點整合的人   │ 工程師 / 有技術背景     │
        │（零學習成本） │                    │（要懂指令、看得懂程式）│
        └──────────────┴──────────────────┴────────────────────────┘

   AI 操作自由度 / 靈活性   低 ───────────────────────────►  高
   本機安全性             高 ◄───────────────────────────   低
   需要的技術背景          低 ───────────────────────────►  高
```

> 🧭 **怎麼選？建議用「你的知識背景」當主要依據**：沒有技術背景就從 web 端開始，最直覺也最安全；等你看得懂指令、需要 AI 動到本機檔案時，再往 desktop / CLI 走。能力越強的工具，往往也要你越懂它在做什麼。

> 💡 一句話：**自由度和安全性是相反的兩端。**
> - **web**：最安全，但 AI 幫你做事的範圍也最有限（碰不到你的檔案）。
> - **desktop app**：折衷，預設關在沙箱裡活動。
> - **CLI / IDE**（如 Claude Code）：能力最強、最靈活，但它握有高權限，能直接讀寫你的程式碼、執行系統指令——也最需要留意你授權了什麼。
>
> CLI 和 IDE 本質上是同一類（都是高權限的開發工具），差別只在使用習慣。背後可以是同一個模型，只是入口和「被授權的權限」不同。

---

## 八、讓 AI 會「做事」與查你的資料

### API
讓**程式**直接呼叫 AI 的接口，不透過聊天視窗。

> 🪟 比喻：餐廳的**外帶窗口**。讓其他系統不用走進店裡（聊天介面），直接來「點餐」拿結果。

### Agent（代理 / 智能體）
不只是回答問題，還會**自己規劃步驟、動手用工具**把任務做完的 AI。

> 🤵 比喻：不只給你建議，還會**幫你跑腿辦事**的助理——查資料、寄信、改檔案一條龍。

### Sub-agent（子代理）
主 Agent 為了分工，**派出去處理子任務**的小助手。

> 👥 比喻：主管把一部分工作**交給專責組員**，各做各的再彙整。

### Skills（技能）
預先封裝好的「能力包」，讓 AI 在需要時叫用，處理特定工作（例如做簡報、查行事曆）。

> 📒 比喻：給助理一本本標好的 **SOP 手冊**，遇到對應狀況就翻出來照做。

### MCP（Model Context Protocol）
一種「**統一插座**」標準，讓 AI 能安全地連到外部工具和資料（行事曆、資料庫、檔案、網頁）。

> 🔌 比喻：AI 世界的 **USB 規格**。有了統一接口，一個孔就能接遍各種裝置，不用每個都客製。

### RAG / Embedding / Rerank
> RAG 的完整原理（切塊、向量、為什麼上傳資料≠訓練）在 `LLM.md` 有詳細圖解，這裡只補充名詞。

- **RAG**：先去你的資料堆裡**撈出相關段落**，再連同問題交給 AI，讓它「看著你的資料」作答。
- **Embedding（向量嵌入）**：把文字轉成一串座標數字，**意思相近的就靠得近**，電腦才能比對「語意」而非字面。
  > 🗺️ 比喻：把每句話放到一張「**意思地圖**」上標座標。「貓」和「小貓」站很近，「貓」和「股票」隔很遠。
- **Rerank（重新排序）**：撈回一堆候選段落後，再用更精細的模型**排出「最相關的前幾名」**。
  > 🏆 比喻：先**海選**撈一大批，再由評審**精挑前三名**送進決賽。

---

## 九、圖像 AI：擴散模型怎麼生圖

### 擴散模型 Diffusion
圖像生成的主流做法：從一團**雜訊**出發，一步步「去噪」，最後變成一張清晰的圖。

> 📺 比喻：從滿是雪花的電視畫面，**慢慢調出一張照片**。

```
一團噪點  →  去噪一點點  →  再去噪  →  ……  →  清晰的成品圖
（起點）                                        （終點）
```

### DiT（Diffusion Transformer）
用 Transformer 架構打造的擴散模型，是近年高品質生圖／生影片模型的核心。

> 🏎️ 比喻：擴散模型**換上一具更強的引擎**，畫質和理解力大幅提升。

### Latent（潛空間）
模型不是直接在「像素」上慢慢算（太耗力），而是在一個**壓縮過的「概念空間」**裡作業，最後再還原成完整圖片。

> 🗜️ 比喻：先在**草稿／縮圖層**快速作業，定案後再放大成成品，省很多力氣。

### VAE
負責在「潛空間（壓縮的概念）」和「真正的像素圖」之間**來回轉換**的元件。

> 📦 比喻：壓縮檔的**打包 / 解壓縮器**。進去先壓縮、出來再還原成看得到的圖。

### TE（Text Encoder，文字編碼器）
把你打的**提示詞**翻成模型聽得懂的訊號，指揮它要生成什麼。

> 📋 比喻：把你的口頭需求，翻成設計師看得懂的**規格書**。

### 噪點 / 去噪 / 噪點表 / 采樣器
這幾個是生圖過程的零件，一起理解最快：

- **噪點 Noise**：雜訊。生圖的起點就是一團噪點。
- **去噪 Denoise**：一步步把噪點清掉、逼近成品的過程。
- **噪點表 Noise Schedule**：規定「每一步該去掉多少噪點」的時間表。
- **采樣器 Sampler**：決定「**怎麼去噪**」的演算法。不同采樣器，速度與風格略有差異。

> 🗿 比喻：**噪點**是一塊大理石原石，**采樣器**是雕刻手法，**噪點表**是進度表，**去噪**就是一刀刀把雕像刻出來。

### ComfyUI
用「**拉節點、連線**」的方式來組裝圖像生成流程的視覺化工具，彈性極高。

> 🔧 比喻：像接**電路圖**一樣，自由組裝你自己的生圖流水線。

---

## 十、其他常見模型（看圖、聽聲音）

### YOLO
即時的「**物件偵測**」模型：在圖上**框出**「這是什麼、在哪裡」。

> 📹 比喻：監視器畫面自動**框出**裡面的人和車。

### SAM 3.1（Segment Anything）
「**影像分割**」模型，跟 YOLO 同屬「看圖」家族，但它不只是框框，而是把物件**沿著邊緣精準切出來**。

> ✂️ 比喻：**YOLO 圈出大概範圍，SAM 沿著輪廓剪下來。** 一個給方框，一個給精準剪影。

### Whisper
把「**語音轉成文字**」的模型（聽寫、自動字幕）。

> 🎧 比喻：一位超強的**聽打員**，講什麼它打什麼。

### TTS（Text-to-Speech）
反過來，把「**文字唸成語音**」。

> 📢 比喻：會朗讀的**播報員**，給它文字就唸出聲音。

### 多模態 Multimodal
能同時處理**多種型態**（文字＋圖片＋聲音＋影片）的模型。第四節的 `mmproj`，就是讓純文字 LLM 補上「看圖」這個模態的零件。

> 🧠 比喻：從只會**讀字**的助理，升級成能**看、能聽**的全能助理。

---


