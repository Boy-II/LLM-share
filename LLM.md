# LLM 概述及 Prompt 技巧

> **LLM 是什麼？** LLM 是 Large Language Model（大型語言模型）的縮寫，就是你平常用的那些 AI 對話工具背後的技術核心。Gemini（Google）、Claude（Anthropic）、ChatGPT（OpenAI）都是 LLM。

> 這份文件不需要任何技術背景。如果你用過 ChatGPT、Gemini、NotebookLM，這裡說的東西跟你直接相關。

---

## LLM 的運作原理：它到底在做什麼？

在談誤解之前，先說清楚一件事：**LLM 的本質，是一個極度複雜的「猜下一個字」機器。**

沒有別的，就是這樣。

### 預測下一個字

你在手機上打字，輸入法會猜你想打什麼，給你幾個建議字。LLM 做的是同一件事——只是它的「猜字能力」建立在閱讀過幾乎整個網路的文字之後，準確度高到讓你覺得它在「思考」。

實際的運作流程是這樣：

```
你輸入：「今天天氣很___」

LLM 根據前面的文字，計算每個字出現的機率：
  「好」  → 40%
  「熱」  → 25%
  「差」  → 15%
  「冷」  → 10%
  其他    → 10%

選出機率最高的字 → 輸出「好」

接著繼續預測：「今天天氣很好，___」
→ 再選出下一個最可能的字
→ 重複，直到回答結束
```

你看到的整段回答，就是這個「猜字 → 輸出 → 再猜下一個字」的過程重複幾百次的結果。

### 這個原理解釋了很多事

理解這個機制之後，後面提到的很多現象就說得通了：

- **為什麼 AI 會「亂說話」（幻覺）？**
  因為它沒有「不知道就停下來」的機制，只能繼續猜，猜出來的不一定是事實

- **為什麼讓 AI「先思考再回答」（CoT）有用？**
  因為它輸出的每個字都會影響下一個字的預測，寫出來的思考步驟會把它引向更準確的結論

- **為什麼 Prompt 寫得好，輸出就會好？**
  因為 Prompt 是它預測的起點，起點設定得越精確，後續的預測方向就越對

帶著這個理解，我們來看幾個常見的誤解。

---

## 誤解一：「我上傳了資料，就是在訓練 AI」

很多人用過 NotebookLM，把公司文件、報告、手冊丟進去，然後就覺得「這樣 AI 就學會我們的東西了」。

**這個理解是錯的。**

### 上傳資料 vs 訓練模型，差在哪？

用一個比喻來說：

```
訓練模型  →  讓一個人從小學廚，花十年學遍各種料理，
             學完之後那些知識就長在他腦子裡了。

上傳資料  →  廚師已經訓練好了，你只是在對話前
             把食譜遞給他看，他邊看邊回答你的問題。
```

NotebookLM 做的是第二件事。你上傳的資料，它只是「在你問問題的時候去翻」，並不是把你的資料學進去。你一關掉對話，它對那份資料就沒有任何印象了。

### 這個機制的正式名稱叫 RAG

RAG 是「先去資料堆裡撈相關內容，再拿來回答」的做法。流程大概長這樣：

```
你的問題
    │
    ▼
┌─────────────────────┐
│  去你上傳的資料裡    │
│  找跟問題相關的段落  │
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│  把找到的段落 +      │
│  你的問題一起交給 AI │
└─────────────────────┘
    │
    ▼
AI 的回答
```

它的知識庫是「放在旁邊供查閱的參考資料」，不是「刻進 AI 腦子裡的東西」。

### 知識庫怎麼找到相關資料的？

AI 不是把整份文件從頭讀到尾，那樣太慢、成本太高。它用的方式叫做**切塊（Chunking）**。

上傳文件的時候，系統會先把文件切成很多小段：

```
原始文件（一份完整的員工手冊）
│
├─ 第 1 塊：公司簡介（約 200 字）
├─ 第 2 塊：請假規定（約 200 字）
├─ 第 3 塊：薪資結構（約 200 字）
├─ 第 4 塊：福利說明（約 200 字）
└─ ......
```

切完之後，每一塊都會被轉換成一串數字（這叫做向量，可以理解成「意思的座標」）。你問問題的時候，系統先找出跟你問題「座標最接近」的幾塊，再把那幾塊拿去給 AI 看。

這就是 RAG 的核心：**不是把整份文件丟給 AI，而是精準撈出最相關的段落。**

### 為什麼中文的切塊效率比較差？

這裡有一個很實際的問題：**這套系統最初是為英文設計的。**

英文的切塊很直覺，因為英文單字之間有空格：

```
英文：The   employee   must   submit   a   leave   request
      ↑每個單字都有明確的邊界，切起來很乾淨
```

中文沒有空格，每個字都黏在一起，而且同一串字可能有完全不同的讀法：

```
一個半小時等於三個半小時

讀法 A：一個半小時（1.5小時）＝ 三個 半小時（3×0.5小時）→ 正確
讀法 B：一個 半小時（0.5小時）＝ 三個半小時（3.5小時）   → 錯誤
```

這句話連人讀起來都可能停頓錯誤、理解錯誤，機器切塊只會更嚴重。

系統在處理中文時，要額外花力氣判斷「詞的邊界」，這個判斷並不是百分之百準確。切錯的話，一個完整的概念可能被切到兩個不同的塊裡，導致你問問題時，系統撈到的段落語意不完整，AI 就可能回答得不夠準確。

**實際影響：**

- 同樣的知識庫，英文文件的查找準確度通常優於中文
- 中文文件如果段落結構清晰（有標題、有分點），切塊效果會好一些
- 把重要概念放在同一個段落裡，盡量不要跨段落分散，可以減少切錯的影響

### 那真正的訓練是什麼？

訓練一個 LLM，大概需要：
- 超多的的文字資料
- 很多顆高階 GPU 同時運算
- 幾個月的時間
- 很多很多的錢

這不是一般公司、一般人能做的事。目前市面上的模型（GPT-4、Gemini、Claude）都是由大型科技公司花這個等級的資源訓練出來的。

**你我能做的，是「使用」這些模型，而不是「訓練」它們。**

---

## 誤解二：「AI 記得我之前說過的話」

很多人用 ChatGPT 或 Claude 一段時間之後，會覺得「它好像認識我了」、「它記得我上次講的事」。

**大部分情況下，這個感覺是假的。**

### AI 的「記憶」實際上是什麼？

LLM 本身沒有記憶這個機制。它能「記得」的東西，只有**這次對話視窗裡的內容**。

```
┌─────────────────────────────────────────┐
│              這次的對話視窗              │
│                                         │
│  你：你好，我叫小明                      │
│  AI：你好小明！                          │
│  你：我喜歡爬山                          │
│  AI：爬山很棒！                          │
│  你：我叫什麼名字？                      │
│  AI：你叫小明。         ← 它「記得」，   │
│                           是因為上面寫著 │
└─────────────────────────────────────────┘

關掉視窗之後 → AI 完全不記得小明是誰
```

它不是真的記住了你，它只是在這個視窗裡「看得到」你說過的話。

### 那為什麼有些工具「好像」有長期記憶？

因為那些工具在背後做了額外的工程：把你的對話紀錄存起來，下次開新對話時，悄悄地塞回給 AI 看。

```
你上次說過的話
      │
      │（工具自動附上）
      ▼
┌──────────────────┐
│   新的對話視窗    │  ← AI 看到這些，才「想起來」
│  + 你的新問題    │
└──────────────────┘
```

這是工具層面的設計，不是 AI 本身有記憶。

### 實例：Gemini 的 Gem 怎麼運作？

Gem 是 Gemini 的「自訂 AI 助理」功能，你可以設定它的角色、口吻、專長範圍。很多人以為開了一個 Gem 就等於訓練了一個專屬 AI，但實際上它做的事情是這樣的：

**每次你開啟 Gem 對話的第一輪，系統在背後自動組合了這些東西：**

```
┌──────────────────────────────────────────────────┐
│               送給 AI 的完整內容                  │
│                                                  │
│  ① 預設 Prompt                                   │
│     你在設定 Gem 時寫的角色說明                   │
│     例：「你是一個專業的HR助理，回答要簡潔...」   │
│                                                  │
│  ② 知識庫                                        │
│     你上傳的文件、手冊、FAQ 等資料                │
│     （AI 在回答前會去翻這些，類似 RAG）           │
│                                                  │
│  ③ 記憶 JSON                                     │
│     Gemini 把你過去跟這個 Gem 的重要對話          │
│     摘要整理成結構化的筆記，自動附上              │
│                                                  │
│  ④ 你這次的問題                                  │
│     你剛剛打的那句話                              │
│                                                  │
└──────────────────────────────────────────────────┘
              │
              ▼
         AI 開始回答
```

**白話說就是：** Gemini 不是一個「有自己記憶的獨立 AI」，它是每次對話開始前，系統把所有相關資料打包好一起丟給 AI 看，AI 才知道自己的角色是什麼、你是誰、之前聊過什麼。

**關鍵差異整理：**

| | 你以為的 | 實際上 |
|---|---|---|
| Gem 的角色設定 | AI 被「改造」成新角色 | 每次對話前附上角色說明 |
| 上傳的知識庫 | AI 學會了這些知識 | 回答時去翻閱，不是記在腦子裡 |
| 偷偷儲存的「記憶」 | AI 真的記得你 | 系統把摘要筆記附在每次對話裡 |

> ⚠️ **清除記憶 JSON 的影響**
>
> Gemini/ChatGPT/Claude 的設定頁面有「清除記憶」的選項。一旦清除，所有 Gemini/ChatGPT/Claude 記錄下來的個人相關資訊都會消失：你的偏好、你說過的背景、你的習慣設定……全部歸零。下次對話，那個AI 對你來說就跟第一次見面一樣。

### 這件事為什麼重要？

因為如果你誤以為 AI 記得你，你就可能：

- 以為它累積了對你的了解，但其實沒有
- 在新對話裡省略重要背景，導致它回答偏掉
- 以為它「認識」你的公司或你的工作內容，但每次都是從零開始

**最穩的做法：每次開新對話，把重要背景重新說一次。**

---

## 誤解三：「LLM 知道最新的事情」

很多人用 ChatGPT 或 Gemini 問時事、問最新的產品資訊、問今天的新聞，然後對 AI 能回答感到驚訝，或者對 AI 答錯感到失望。

這背後有一個很基本的事實很多人不知道：**LLM 是有知識截止日期的。**

### 什麼是訓練截止日期？

訓練一個 LLM 的時候，要餵給它大量的文字資料。這批資料是在某個時間點前收集的，訓練結束之後，模型就「定型」了——它只知道那個時間點之前存在的資訊。

```
訓練資料收集截止（例如 2024 年初）
        │
        ▼
    模型訓練完成
        │
        ▼
    模型上線給你用（例如 2024 年底）
        │
        ▼
    你在 2025、2026 年繼續使用
        │
        ▼
  模型對截止日之後發生的事情一無所知
```

你問它「最近有什麼 AI 新消息」，它給你的可能是一兩年前的資訊，而且它不會主動告訴你這件事。

### 那為什麼 AI 有時候能回答當前的資訊？

這跟模型本身沒有關係，而是因為工具在背後做了額外的事情。

當你問 ChatGPT 或 Gemini 一個需要即時資訊的問題，它實際上送給 AI 的不只是你的問題，而是這樣：

```
Prompt（角色設定）
      +
網路搜尋結果（工具自動去搜尋，把結果附上）
      +
你的請求
      ║
      ▼
    AI 的回應
```

AI 是讀了搜尋結果之後，再用自己的語言整理給你看。它不是「知道」這件事，而是「剛才查到」這件事。

> ⚠️ **實際影響**
>
> - 沒有開啟網路搜尋的對話，AI 只能根據訓練截止前的知識回答
> - AI 可能用自信的語氣說出過時的資訊，因為它不知道自己不知道
> - 需要即時資訊的問題（股價、新聞、最新法規），一定要確認工具是否有開啟搜尋功能

💡 **自己試試看：** 打開你常用的 AI 工具，直接問它：

```
你的訓練知識截止時間是什麼時候？
```

它會告訴你它的知識更新到哪一天。這個日期之後發生的事情，它在沒有網路搜尋的情況下一概不知。

但這裡有一個關鍵問題：**LLM 不會說「我不知道」然後停下來。它會強迫自己給出一個答案。**

它的運作方式就是不斷預測「下一個最合理的字」，即使面對它根本沒有資料的問題，它也會用現有的知識拼湊出一個「聽起來合理」的回答——哪怕那個回答是錯的。

這就是大多數人說的**幻覺（Hallucination）**：AI 不是在說謊，它只是在做它唯一會做的事——預測，然後輸出。它沒有能力偵測「這個問題超出我的知識範圍，我應該停止回答」。

```
你問：「今年最新的 XX 法規是什麼？」

AI 的實際狀況：
  訓練截止後的資料 = 空白
  但它不能輸出空白
  所以它用已知的舊資料 + 語言預測 → 給你一個答案

你拿到的：一個語氣自信、但可能完全錯誤的回答
```

> ⚠️ 幻覺最危險的地方不是 AI 說錯，而是它說錯的時候**語氣跟說對的時候一模一樣**。你沒有辦法從語氣判斷它是否在胡說。涉及重要決策的資訊，一定要自行查證來源。

---

## 小結：AI 的回應是可以被設計的

把前面說的整理起來，AI 回答你的依據其實就是這個公式：

```
Prompt（角色與規則設定，可以直接包含參考資料）
      +
知識庫（選用，適合資料量大的情況）
      +
記憶 JSON（過去對話的摘要紀錄）
      +
你的請求（這次輸入的問題）
      ║
      ▼
    AI 的回應
```

> 📌 **知識庫不是必要的。** 如果參考資料不多，直接寫進 Prompt 裡效果一樣，有時候還更穩定——因為省去了切塊和撈取的步驟，AI 能看到完整的內容，不會因為切塊切錯而漏掉重要資訊。

這代表一件很重要的事：**AI 的回應不是隨機的，而是可以被設計的。**

你改變其中任何一個輸入，輸出就會跟著改變。其中影響最大的是 **Prompt 的編寫**——它決定了 AI 用什麼身份、什麼邏輯、什麼格式來回答你。寫得好的 Prompt 跟隨手打的問題，拿到的回答品質可以差很多。

> 💡 **小補充：AI 的回答可以 100% 完全復現**
>
> 只要四個條件完全相同：**Prompt 相同、參數相同（例如溫度 Temperature）、種子（Seed）相同、請求相同**，AI 就會給出一模一樣的回答，沒有例外。AI 不是在「靈感作答」，它是在做有跡可循的計算。這也是為什麼好的 AI 應用會把 Prompt 當成核心資產來管理。

---

## 如何寫好一個 Prompt

前面說了 Prompt 是影響 AI 輸出最重要的因素。那實際上怎麼寫？

### 第一步：先想清楚這兩件事

在動手寫之前，先問自己：

```
1. 我的要求是什麼？     → 我想要什麼樣的結果
2. 我要它幹什麼？       → AI 扮演什麼角色、做什麼動作
```

沒想清楚就開始寫，很可能導致：

- **過多不必要的修改** ── 寫完發現方向錯了，從頭來過
- **Prompt 越來越臃腫** ── 一直補東西卻沒有刪掉沒用的
- **輸出不符合預期** ── AI 理解的跟你想的不一樣
- **Prompt 內部矛盾** ── 前面說一件事，後面又說相反的

所以動筆前，先把這兩個問題想清楚，再開始寫。

---

### 第二步：寫的時候注意三件事

想清楚目的之後，Prompt 通常已經能達到你要的基本效果。但要讓輸出更準確、更穩定，寫的時候還要注意：

**① 不寫對目的沒幫助的內容**
廢話會讓 Prompt 臃腫，浪費 AI 的注意力，也讓你自己更難調試。

**② 不寫模糊的要求**
「整理一下」、「寫得好一點」、「簡短」都是模糊的說法。
AI 不會猜你的意思，要寫清楚「條列三點」、「不超過 100 字」這種具體的描述。

**③ 盡可能給一個示例**
這是最省力、效果最明顯的做法。
與其花三句話解釋你要什麼格式，不如直接給一個範例，AI 照著做就對了。

> 📌 **關於 Prompt 長度：** 根據個人測試，Prompt 在 **4,000～6,000 token** 左右效果最好。太短說不清楚，太長 AI 的注意力會被稀釋，反而抓不住重點。

---

### 第三步：寫完之後，做一次檢查

Prompt 寫完不代表完成。上線前建議做一輪檢查：

- 有沒有**模糊的詞**？（「幫我整理一下」是什麼意思？）
- 有沒有**互相矛盾的要求**？（「簡短但詳細」這兩件事同時要？）
- 有沒有給**至少一個格式示例**？
- 實際測試幾次，確認輸出穩定

---

### 示例：叫 AI 幫你整理會議紀錄

我們用一個真實情境來走一遍 Prompt 的迭代過程。

**第一版（隨手打）**

```
幫我整理會議紀錄
```

AI 可能給你一篇通順的文章，但格式不對、重點不對、字數太多或太少。問題是你什麼都沒說清楚。

---

**第二版（加上角色和要求）**

```
你是一個專業的行政助理，請幫我整理以下會議紀錄，
條列出討論事項和決議結果。
```

好多了，但你可能發現 AI 把不重要的閒聊也列進去了，
而且格式跟你平常用的不一樣。

---

**第三版（加上格式規範和排除條件）**

```
你是一個專業的行政助理。
請整理以下會議紀錄，規則如下：
- 只保留討論事項、決議、和負責人
- 閒聊與題外話一律略過
- 每個決議後面標注負責人與預計完成日期
- 格式參考如下：

【討論事項】XXX
【決議】XXX
【負責人】XXX ｜ 【期限】XXX
```

這樣 AI 拿到的指令夠具體，輸出就會跟你預期的接近很多。

---

**這個迭代過程說明了一件事：**

> Prompt 不是一次就寫好的。你根據 AI 的輸出結果，找到它哪裡沒理解你的意思，然後補上去。每改一次，輸出就更準一次。

寫完 Prompt 之後，記得檢查：
- 有沒有模糊的詞（「幫我整理一下」是什麼意思？）
- 有沒有互相矛盾的要求
- 有沒有給至少一個格式示例

---

### 格式比你想的更重要

AI 讀你的 Prompt，就像你讀一份沒有標點的長文——結構越亂，它越難抓到重點。

**格式效果由高到低大致是：**

```
JSON  >  YAML  >  XML 標籤  >  Markdown 標題/條列  >  大段白話文
```

> 📌 **模型差異：Claude 是例外**
>
> 以上排序適用於大多數模型。但 Claude 的情況相反——**XML 是 Claude 理解效果最好的格式，JSON 反而是最差的。**
> 使用 Claude 時，優先選擇 XML 結構。

**用白話文寫（效果差）：**
```
你是行政助理請幫我整理會議紀錄只保留討論事項決議和
負責人閒聊略過每個決議後面標注負責人與期限
```

**用 Markdown 條列寫（效果好）：**
```
你是一個行政助理，請整理會議紀錄：
- 只保留討論事項、決議、負責人
- 閒聊略過
- 每個決議標注負責人與期限
```

**用 XML 標籤寫（效果最穩定）：**
```xml
<角色>行政助理</角色>
<任務>整理會議紀錄</任務>
<規則>
  <規則1>只保留討論事項、決議、負責人</規則1>
  <規則2>閒聊略過</規則2>
  <規則3>每個決議標注負責人與期限</規則3>
</規則>
```

XML 的優勢在於每個概念都被包在自己的標籤裡，AI 不容易把不同區塊的內容混在一起理解。

---

### 用英文寫 Prompt 效果更好

這是一個有根據的建議：**同樣的意思，用英文寫的 Prompt，AI 的理解通常比中文更準確、更穩定。**

原因跟前面說的訓練資料有關——這些模型吃的文字絕大多數是英文，英文的語義邊界對它來說比中文清晰得多。

> ⚠️ **常見誤區：Prompt 的語言 ≠ 輸出的語言**
>
> 很多人以為用英文寫 Prompt，AI 就會用英文回答；用中文寫，AI 就用中文回答。
>
> **這是錯的。**
>
> Prompt 的語言和輸出的語言是兩件獨立的事。你完全可以用英文寫 Prompt，同時要求 AI 用繁體中文輸出：
>
> ```
> You are a professional meeting assistant.
> Summarize the meeting notes below.
> Rules:
> - Keep decisions and owners only
> - Output in Traditional Chinese
> ```
>
> AI 會用英文理解你的指令，然後用繁體中文回答你。兩者互不衝突。

---

## 進階提示詞編寫

學會基礎之後，我們來看更深一層的思路。

### 提示詞的本質：縮小輸出範圍

進階的理解方式是這樣的：

**Prompt 的作用，是把 AI 可能輸出的內容範圍壓縮到你要的那個區間。**

```
沒有 Prompt：
AI 可能輸出 100 種不同的內容
符合你要求的只有 5 種 → 很難命中

有 Prompt：
AI 可能輸出的範圍被壓縮到 6～10 種
符合你要求的有 4～5 種 → 輕鬆命中
```

所以寫 Prompt 不是在「跟 AI 說話」，而是在**劃定邊界**——把你不要的輸出一一排除掉。

---

### 進階編寫前，多問自己兩件事

除了基礎篇的「要什麼」和「要它幹什麼」，進階還要考慮：

```
3. 這個模型有什麼特性？   → 不同模型對格式、語言的反應不一樣
4. 注意力要怎麼分配？     → 什麼資訊最重要，要放在哪裡
```

**什麼是注意力？**

你可以這樣理解：開一個很長的會議，或者讀一篇很長的文章，結束之後你最容易記得的通常是**開場說的事**和**最後的結論**，中間講了什麼反而最模糊。

AI 讀 Prompt 的方式跟這個非常像。這個機制在技術上叫做「注意力（Attention）」，它決定了 AI 在回答時，對哪些內容給予更多權重。

```
┌─────────────────────────────────┐
│  Prompt 開頭   ← 注意力最高     │
│  ...                            │
│  中間內容      ← 注意力較低     │
│  ...                            │
│  Prompt 結尾   ← 注意力次高     │
└─────────────────────────────────┘
```

**關於注意力分配：**
大部分模型對 Prompt **開頭和結尾** 的指令遵循度最高，中間的內容相對容易被忽略。
最重要的規則放開頭，最後再用一句話重申核心任務，效果會比把所有東西堆在中間好。

---

### CoT 是什麼？

CoT 全名是 Chain of Thought，中文叫「思維鏈」。概念很簡單：**讓 AI 把推理的過程寫出來，而不是直接跳到結論。**

為什麼這樣有用？

前面說過，AI 是「邊預測邊輸出」的，它生成的每一個字都是基於前面已經輸出的內容預測出來的。這代表一件事——**它輸出的文字，會反過來影響它接下來的輸出。**

如果讓它直接給結論，它可能走一條「看起來合理但其實有問題」的捷徑。但如果讓它先把推理過程寫出來，這些寫出來的步驟就會成為後續回答的依據，把它拉回正確的軌道。

```
沒有 CoT（直接跳結論）：

  你的問題 ──→ [黑盒子] ──→ 答案
                              ↑
                    可能走了一條你看不見的歪路

有 CoT（先思考再回答）：

  你的問題 → 步驟一 → 步驟二 → 步驟三 → 答案
                ↑         ↑         ↑
          每一步都可以被檢查，走偏了會被拉回來
```

就像你解數學題先打草稿，比直接在腦子裡算，出錯的機率低很多。

CoT 是少數使用者可以不需要任何技術背景、直接提升模型輸出品質的方法，非常值得花時間學。

> ⚠️ **關鍵：沒有輸出的 CoT 幾乎沒有效果**
>
> CoT 起作用的原因是：AI 在預測下一個 token 時，對越近的內容權重越高。思考步驟寫出來，就成為後續輸出的依據。
>
> **如果你要求 AI「在心裡想一想再回答」，但不要求它把思考過程寫出來，效果幾乎為零。** CoT 一定要讓它輸出才有用。

**實際用法：**

```
沒有 CoT：
請分析這份合約有沒有風險。
→ AI 直接輸出結論，可能漏掉某些條款

加上 CoT：
請先逐條列出你認為需要注意的地方，
確認完畢後再給出整體的風險評估。
→ AI 先整理每條條款，思路清楚後再下結論
```

**CoT 的編寫原則：**

- **不自相矛盾** — 思考步驟之間不能打架，否則 AI 會不知道該遵循哪一步
- **影響越大的問題放越前面** — 決定整體方向的步驟先寫，細節後補

---

### 另一種 CoT 思路：規劃注意力

一般的 CoT 是讓 AI 思考推理過程。但還有另一種用法——**不要求 AI 思考，而是要求它先找到相關資料，再開始輸出。**

```
逐步確認以下內容，隨後開始輸出：
- 這個任務的類型是什麼？
- 與這個任務相關的資料或背景是什麼？
- 依據以上內容，應該如何完成任務？
```

這種做法的邏輯是：讓 AI 先定位自己在「圖書館的哪個區」、手邊有哪些材料，確認完才開始寫。類似人在動筆前先翻資料、確認方向，而不是直接憑印象輸出。

---

### 主 CoT + 子 CoT：複雜任務的結構

對於長任務或多步驟任務，不一定要把 CoT 全部集中在 Prompt 開頭。更有效的做法是在每個關鍵節點插入子 CoT，讓 AI 在每次不確定時重新確認方向：

```
主 CoT（引導整體輸出方向）
────────────────────────
模型輸出①（任務第一部分）
────────────────────────
子 CoT（依據輸出①，反思輸出②的走向）
────────────────────────
模型輸出②（任務第二部分）
────────────────────────
子 CoT（再次確認）
────────────────────────
模型輸出③ ...
```

這樣的結構讓 AI 在每個階段都有機會「重新校正」，避免長任務越寫越偏的問題。

---

### 迭代思考：讓 AI 挑戰自己的結論

CoT 是讓 AI 把推理過程寫出來。**迭代思考**則是在此基礎上更進一步——讓 AI 主動挑戰自己上一輪的結論，逼它提出更好的答案。

概念是這樣的：

```
第一輪：提出一個想法
  │
  ▼
第二輪：挑戰這個想法
        「這個結論有沒有問題？有沒有更好的做法？」
  │
  ▼
第三輪：基於挑戰，提出更完善的結論
  │
  ▼
（視需要繼續迭代）
```

這跟人在開會時做的事一樣：先有人提案，再有人質疑，最後大家得出比最初提案更好的結果。差別是這裡提案和質疑都是同一個 AI 在做。

**為什麼這樣有效？**

AI 的第一個回答往往走的是「最順的路」——最常見、最不出錯的方向。迭代思考強迫它停下來問自己：「這個答案真的夠好嗎？」這個自我挑戰的過程，往往能發現第一輪沒注意到的盲點。

**Prompt 寫法示例：**

```
請用以下流程回答這個問題：

第一輪：給出你的初步想法與理由
第二輪：挑戰你在第一輪的結論，指出潛在的問題或不足
第三輪：整合以上內容，給出你認為更完善的最終答案
```

**什麼時候用迭代思考？**

- 需要批判性分析的任務（策略規劃、風險評估、文章審查）
- 你懷疑 AI 的第一個答案可能太表面
- 希望 AI 的結論是經過自我檢驗的，而不是第一直覺

---

### 實戰：用 XML 標籤建立結構化 Prompt

前面說過 XML 格式能讓 AI 更準確地理解你的意圖。這裡分享一套實際在用的標籤結構，每個標籤用簡單明確的英文單字來切割不同的功能區塊。

**標籤說明：**

| 標籤 | 用途 |
|---|---|
| `<system>` | 整個 Prompt 的總框架，定義這個 AI 的整體設定 |
| `<role>` | AI 扮演什麼角色，身份與立場（同時引導模型的注意力方向） |
| `<skill>` | 這個角色具備哪些專業能力或知識範圍 |
| `<cot>` | 思考流程的規則，要它怎麼思考、先做什麼再做什麼 |
| `<turn>` | 對話的起始點或任務啟動指令 |
| `<guideline>` | 行為準則，什麼可以做、什麼不可以做 |
| `<output_format>` | 輸出的格式規範，要用什麼結構呈現答案 |
| `<checklist>` | 輸出前的自我檢查清單，同時把注意力從結尾拉回中間的 CoT |

**完整結構示例（以會議紀錄助理為例）：**

```xml
<system>
  <role>
    You are a professional administrative assistant
    specialized in meeting documentation.
  </role>

  <skill>
    - Extracting key decisions from unstructured text
    - Identifying action items and owners
    - Writing concise, structured summaries
  </skill>

  <guideline>
    - Include only decisions, action items, and owners
    - Skip small talk and off-topic discussions
    - Do not add information not present in the source
  </guideline>

  <cot>
    Before writing the summary:
    1. Read through the full meeting notes
    2. Identify all decisions made
    3. Identify all action items and their owners
    4. Check for missing owners or unclear deadlines
    Then produce the output.
  </cot>

  <output_format>
    【決議】{decision}
    【負責人】{owner} | 【期限】{deadline}
    Output in Traditional Chinese.
  </output_format>

  <checklist>
    - Every decision has a clear owner
    - Every action item has a deadline
    - No off-topic content included
  </checklist>
</system>

<turn>
  以下是今天的會議紀錄，請開始整理：
  {會議紀錄內容}
</turn>
```

**`<role>` 跟注意力的關係**

`<role>` 不只是在告訴 AI「你要扮演誰」，它同時在引導 AI 把注意力集中到哪一塊知識上。

你可以把 LLM 想像成一座巨大的圖書館——它在訓練過程中讀過幾乎所有領域的文字。但這座圖書館沒有人帶路的話，它不知道你現在需要哪個區的書。

```
沒有 role（圖書館沒有帶路）：
整座圖書館都是候選 → AI 什麼都能說，但不夠專精

有 role（帶你去對的區）：
「你是一位專業編輯，負責內容審核及文章用詞」
  → AI 的注意力集中到編輯、寫作、語言表達相關的知識區塊
  → 回答會更專業、更符合編輯視角，而不是泛泛而談
```

越具體的 role，帶路效果越好：

| role 的描述 | 注意力範圍 |
|---|---|
| 你是一個助理 | 太廣，幾乎沒有縮小範圍 |
| 你是一個編輯 | 縮小到語言、文字領域 |
| 你是一個負責內容審核及文章用詞的專業編輯 | 精確定位到校稿、遣詞、品質把關 |

描述越具體，AI 拉來的「書」就越對，回答就越到位。

---

**為什麼這樣寫有效？**

每個標籤都是一個獨立的「概念容器」。AI 讀到 `<role>` 就知道這裡是身份設定，讀到 `<guideline>` 就知道這裡是行為規則，不會把不同區塊的內容混在一起解讀。

這就像一份格式清晰的工作說明書——條列清楚、責任分明，執行的人不需要猜你的意思。

> 💡 **`<checklist>` 的進階技巧：把注意力拉回 CoT**
>
> 前面說過，AI 的注意力在 Prompt 的開頭和結尾最高，中間最低。
>
> 問題在於，`<cot>` 通常落在 Prompt 的中間——那正是注意力最薄弱的位置。AI 讀完 `<cot>` 之後繼續往下讀，到了輸出的時候，對 CoT 的遵循度可能已經打折了。
>
> `<checklist>` 的作用就是利用結尾的高注意力，**把 AI 的注意力重新導回 CoT**。
>
> ```
> Prompt 結構：
>
> <system>          ← 注意力高（開頭）
>   <role>
>   <skill>
>   <cot>           ← 注意力低（中間）⚠️
>   <guideline>
>   <output_format>
>   <checklist>     ← 注意力高（結尾）✅ 在這裡要求 AI 回頭確認 CoT
> </system>
> ```
>
> `<checklist>` 裡的內容，本質上是用結尾的注意力優勢，強迫 AI 重新審視它在中間可能忽略的思考規則。這是一個用結構補救注意力衰減的技巧。

> 📌 **標籤名稱用簡單英文的原因：**
> 前面提過英文 Prompt 理解效果更好。標籤名稱本身也是 Prompt 的一部分，用清楚、常見的英文單字（role、skill、guideline），比用中文或自創縮寫，AI 的理解更準確。

---

### `<summary>`：給多輪對話的簡易記憶

前面說過 LLM 本身沒有記憶，對話一關就忘。但有些任務需要跨多輪推進——例如逐步討論一份企劃、分階段審查一份文件。

這時候可以在 Prompt 裡加入 `<summary>` 標籤，要求 AI 在每一輪回答結束後，自己整理一份當前狀態的摘要。下一輪對話時，把這份摘要附上，AI 就能「接著上次的進度繼續」。

**運作方式：**

```
第一輪對話
  │
  ▼
AI 回答 + 自動產出 <summary>
  ┌─────────────────────────────┐
  │ <summary>                   │
  │   已確認：XXX               │
  │   待處理：XXX               │
  │   當前結論：XXX             │
  │ </summary>                  │
  └─────────────────────────────┘
  │
  │  你把這份 <summary> 貼到下一輪的開頭
  ▼
第二輪對話（AI 讀到摘要，知道現在在哪）
```

**Prompt 寫法：**

```xml
<system>
  <role>你是一個企劃審查助理</role>
  <guideline>
    每次回答結束後，輸出一份 <summary> 記錄當前進度，
    包含已確認的部分、待討論的問題、以及當前結論。
  </guideline>
</system>

<summary>
  <!-- 第一輪留空，之後每輪把 AI 產出的 summary 貼在這裡 -->
</summary>

<turn>
  以下是第一個需要討論的部分：...
</turn>
```

**跟 MVU 的差別：**

`<summary>` 是輕量版的狀態管理——只維護一份滾動摘要，不需要定義完整的變數結構。對大多數多輪任務來說已經夠用，門檻也低很多。需要更精細的狀態控制時，才需要升級到 MVU 模式。

---

## 更進階的 Prompt 工程技巧

以上內容涵蓋了日常使用 LLM 所需的核心觀念與 Prompt 編寫技巧。如果你已經掌握這些內容，並且面對更複雜的任務需求，以下是下一個層次的方向：

- **MVU 模式**（Model-View-Update）：使用狀態變數讓 AI 在多輪對話中維護進度與記憶
- **狀態變數**：在 Prompt 中定義變數，讓 AI 在每一輪輸出後更新當前狀態
- **偽代碼寫法**：以類似程式邏輯的結構描述條件與分支，讓複雜任務的規則更精確

如有需求，請向原作者提出。

---

*（持續更新中）*
